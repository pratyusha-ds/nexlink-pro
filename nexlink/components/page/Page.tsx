import { ReactNode } from "react";

// TODO: create a reusable page to have consistent padding and margin
function Page({ children }: { children: ReactNode }) {
  return <div></div>;
}

export default Page;
